package com.lfq06.arknightsreader.format.api

/**
 * Placeholder for the format importer contract.
 *
 * Volume 1 Task 3 fills this module with the importer/reader interfaces
 * (parse capabilities, format detection, import results). This marker keeps
 * the module compilable so downstream modules can depend on it now.
 */
object FormatApiPackageMarker